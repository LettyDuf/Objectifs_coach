/**
 * @deprecated 2026-06-21 — remplacé par `Challenge.tsx` générique (paramétré par type).
 * Conservé vide pour éviter une suppression de fichier (contrainte de permissions
 * du dossier partagé). À retirer définitivement lors d'une session ultérieure.
 */
export {};
