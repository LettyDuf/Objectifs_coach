# STATUS — Coach Objectifs

_Dernière mise à jour : 2026-06-23_

## Phase actuelle
Onglet **Théorie complet sur les 3 modules**. Étoffement contenu pédagogique terminé :
- **Sprint** : 4 thèmes / 10 fiches (Posture+6 transverse + Fondamentaux 4 + Pièges 4 + Cas particuliers 3) + 6 transverses = 16.
- **PI** : 5 thèmes / 9 fiches techniques + 6 transverses = 15.
- **OKR équipe** : 3 thèmes / 7 fiches techniques (Fondamentaux 3 + Pièges du KR 4) + 6 transverses = 13.
Briques (option C, 5 couleurs) en place pour Sprint Goal, PI Objective et Key Result. Pattern maître-détail (sidebar + contenu) sur ThemeDetail. 90/90 tests verts. Build monofichier 517 Ko / gzip 142 Ko.

## Phase précédente
Incrément 2 livré : UI fonctionnelle Sprint (sélecteur → mode → entraînement / apprendre → export). 39 tests verts, build production OK. Premier vrai outil utilisable en atelier sur le périmètre Sprint × dev.

## Fait
- Cadrage produit du V1 + 12 décisions structurantes + DOMAINE validé.
- **Scaffolding** : Vite + React 18 + TS strict + Vitest + ESLint + Prettier.
- **Domaine** : types, ports, heuristiques, 5 critères tronc commun, pénalité composite, calibrage par type (D10).
- **Contenu** : listes verbes/mots flous + 7 exemples annotés Sprint × dev.
- **UI fonctionnelle** :
  - Tokens de design (couleurs sémantiques vert/ambre/rouge WCAG AA, typo lisible atelier, focus visibles).
  - Écran d'accueil — sélection du type (Sprint actif, PI/OKR visibles « à venir »).
  - Écran de sélection du mode — Apprendre / S'entraîner.
  - Écran S'entraîner — formulaire + évaluation en direct, chips colorées avec icônes (couleur jamais seule porteuse d'info).
  - Écran Apprendre — galerie d'exemples annotés avec révélation progressive du diagnostic.
  - Fil d'Ariane navigable.
- **Export** : Markdown / JSON / presse-papier. Adaptateur pur testé.
- **Tests** : 39/39 verts (12 heuristiques + 16 critères + 8 moteur + 3 export).
- **Build production** : OK, 162 KB JS / 52 KB gzippé, 7 KB CSS / 1.7 KB gzippé.
- **Frontière hexagonale** : aucun import React dans `src/domain/`. Vérifié.

## En cours
- Rien.

## Prochain pas (au choix)
A. **Module OKR** : Objective + 3-5 KR + règles spécifiques + UI multi-KR. Termine le périmètre V1 complet (Sprint + PI + OKR).
B. **Défi PI** : étendre le mode ludique restant au PI (corpus mauvais exemples PI à enrichir).
C. **Refactor `PuzzleBoard` générique** : extraction de la mécanique commune Sprint/PI dans un composant unique. Préalable utile avant OKR si OKR récupère aussi un puzzle.
D. **Enrichir le contenu PI** : plus d'exemples annotés pour le mode Apprendre et Défi.
E. **Quiz « Diagnostic en aveugle »** dans Apprendre.
F. **Vague 3 UX** (polissage) : mode plein écran, chip mode actif, toggle densité.

Recommandation : A (OKR) pour atteindre le périmètre V1 complet. Si OKR doit avoir un puzzle multi-KR, faire C avant pour éviter la triplication.

## Risques actifs
- **Scope V1 large** : trois types d'objectifs (Sprint, PI, OKR) couverts dès le V1. Mitigation : livraison par modules, Sprint en premier, démontrable indépendamment.
- **Charge de validation contenu** : la qualité pédagogique dépend des exemples annotés. Mitigation : commencer petit (3 bons + 3 mauvais par module suffisent au V1), enrichir ensuite.

## Journal des sessions
- **2026-06-20** : cadrage initial, création des 4 fichiers d'état. Aucun code écrit.
- **2026-06-20** : précision de vision — l'outil cible plusieurs niveaux hiérarchiques (dev, PO, PM, gestionnaire). V1 = dev uniquement, axe audience modélisé dès la conception (D8). ROADMAP et DECISIONS mis à jour.
- **2026-06-20** : règle UX — parcours mono-type, sélection en entrée d'atelier (D9). Pas de mélange Sprint/PI/OKR à l'écran. ROADMAP et DECISIONS mis à jour.
- **2026-06-20** : DOMAINE validé. Sept points tranchés (journal section 7). Décisions ajoutées : D10 (calibrage ambition par type) et D11 (score business PI = métadonnée). Pré-requis du codage levés.
- **2026-06-20** : scaffolding + premier incrément codé. Moteur tronc commun + module Sprint dev opérationnels. 36/36 tests verts. Build production OK. README rédigé.
- **2026-06-20** : incrément 2 — UI fonctionnelle Sprint complète. Tokens de design, 4 écrans, EvaluationPanel temps réel, ExportPanel (MD/JSON/clipboard), galerie d'exemples avec révélation progressive. 39/39 tests verts. Frontière hexagonale vérifiée.
- **2026-06-20** : packaging monofichier (D12). `coach-objectifs.html` (~170 KB) à la racine, ouvrable en double-clic, zéro install.
- **2026-06-20** : dimension ludique — ajout du mode **Défi** (« Réécris ce mauvais objectif » avec passage au suivant à partir de 80) et d'un **compteur de session** dans l'en-tête (animation discrète, respecte `prefers-reduced-motion`). 45/45 tests verts. Critique flaggée : un score n'est pas un jeu — on avait sous-livré sur le ludique. Corrigé.
- **2026-06-20** : retour terrain Lætitia — deux pathologies majeures identifiées (objectif rétrospectif, sprint maintenance sans valeur). Ajout section 6 bis dans DOMAINE.md avec 12 exemples candidats.
- **2026-06-20** : 3e pathologie identifiée — équipes qui produisent N objectifs distincts (≠ rétrospectif cumulatif). Doctrine retenue : **un seul Sprint Goal** (Scrum Guide 2020, Kniberg, Cohn) consignée en §6 bis.0. Outillage V1 : fiche pédagogique seule (pas de refactor multi-saisie).
- **2026-06-20** : intégration code après validation Lætitia. Heuristiques enrichies (4 verbes + 1 mot flou). 7 nouveaux exemples annotés Sprint (3 rétrospectif + 4 maintenance bons + 3 maintenance mauvais). Nouveau type de contenu `PedagogicalSheet` + 2 fiches (« Pourquoi un seul Sprint Goal », « Sprint de maintenance : trouver la valeur »). Composant SheetCard avec divulgation progressive. 55/55 tests verts. Rebuild monofichier (183 Ko).
- **2026-06-20** : retour Lætitia — le mode Défi n'est pas le « puzzle » qu'elle visualisait. Vrai puzzle = assemblage de blocs drag-and-drop. Décision D14 consignée (dnd-kit, 3 niveaux, validation par score). Spec et corpus initial proposés en §6 ter de DOMAINE.md.
- **2026-06-20** : Lætitia demande de **découper les catégories 2 et 4** et d'ajouter une **tuile à remplir d'un chiffre** dans la variation. Spec remaniée : 6 catégories au lieu de 4. Corpus remanié et reproposé en §6 ter.
- **2026-06-20** : corpus validé en bloc, implémentation puzzle livrée. `@dnd-kit/core` + `sortable` ajoutés. Sous-domaine `src/domain/puzzle/` (types + logique d'assemblage pure, 10 tests). 4e mode dans ModeSelector. Écran `SprintPuzzle` avec drag-and-drop accessible (clavier OK), tuiles à chiffre intégrées, 3 niveaux, compteur de session bumpé à chaque puzzle résolu (score ≥ 80). 65/65 tests verts. Monofichier 242 Ko (gzippé 78 Ko).
- **2026-06-20** : retour Lætitia — usage 100 % laptop, mieux exploiter l'espace. Deux sous-agents experts (design/UX + accessibilité WCAG 2.1 AA) ont produit deux audits complets, archivés dans `audits/`. Plan en 3 vagues consigné en D15.
- **2026-06-20** : Vague 1 livrée. Tokens étendus (7 paliers typo, deux largeurs de contenu narrow/wide 1320 px). Layout `split-layout` (formulaire / EvaluationPanel sticky droit) appliqué à Practice et Challenge. Refonte Puzzle en layout 2 colonnes (source / aside sticky). `.section-title` border-bottom. `.btn--lg` sur CTAs primaires. **4 bloquants a11y résolus** : `role="listitem"` retiré, radiogroup natif, séparation handle/draggable, alternative clavier explicite (bouton « + » à côté de chaque bloc source) + announcer @dnd-kit. Skip link, focus visible sur primary, `aria-live` ciblé sur résumé évaluation seulement, badge session sans remount, `aria-disabled` au lieu de `disabled`. 65/65 tests verts. Monofichier 247 Ko (gzippé 79 Ko).
- **2026-06-20** : Vague 2 livrée. **UX** : barre de score visuelle (`role="progressbar"`, couleur sémantique selon statut), galerie d'exemples Learn en 2 colonnes Bons | Mauvais, regroupement confiance + checkboxes en `<fieldset class="field-group">` 3 colonnes dans Practice et Challenge. **A11y sérieux** : SheetCard pattern Disclosure (h3 sorti du button), labels contextuels des champs numériques Puzzle (« Valeur de départ » / « Valeur cible » / « Facteur » selon template au lieu de « position N »), ExportPanel live region persistante. 65/65 tests verts. Monofichier 250 Ko (gzippé 80 Ko).
- **2026-06-20** : trois retours utilisatrice corrigés. (1) Superposition à droite : sticky allégé sur Practice/Challenge (suppression `max-height` + `overflow-y`), sticky retiré sur Puzzle (panneau trop dense). Breakpoint passage 1 colonne remonté à 1280 px. (2) Couleur trop forte : `--color-focus` passe de `#2563eb` à `#1e3a8a` (bleu marine sobre, contraste WCAG ~10:1 sur blanc). (3) Niveaux Puzzle Facile/Moyen jugés inutiles : sélecteur de niveau supprimé, un seul corpus utilisé (le plus complet, ex-« Difficile »). 65/65 tests verts.
- **2026-06-20** : la superposition persistait dans la zone d'assemblage du Puzzle (capture utilisatrice). Cause identifiée : `flex-wrap` + `horizontalListSortingStrategy` produit des `transform` résiduels qui font déborder les blocs. Correctifs : `.puzzle-target` passe en `align-items/align-content: flex-start` + `position: relative` ; `SortableContext` passe en `rectSwappingStrategy` (stable en wrap). **Bug pédagogique** détecté en passant : « Améliorer » en tête d'objectif n'était pas pénalisé comme verbe d'output (uniquement comme mot flou). Ajouté à `outputVerbs` — un objectif commençant par « Améliorer » est désormais pénalisé doublement (outcome + falsifiable). 65/65 tests verts.
- **2026-06-20** : retour utilisatrice — outil « triste et formel », et Apprendre Sprint mal calibré pour laptop. (1) Learn passe en largeur wide (1320 px), max-width 760 px conservée sur le contenu interne des fiches (lisibilité). (2) Direction esthétique : couleurs plus vivantes + pictogrammes SVG inline (refus des animations / formes arrondies, doctrine sobre maintenue). Palette retravaillée : fond crème `#fbf9f5`, sable `#f5f1ea`, texte brun foncé `#27241d`, accent terracotta `#c2410c` pour les pictogrammes, sémantiques re-saturées. Composant `<Icon>` avec 11 pictos line-art monochromes (Sprint, PI, OKR, Apprendre, S'entraîner, Défi, Puzzle, target, wrench, good, warn, bad). Affichés dans TypeSelector, ModeSelector, fiches pédagogiques. 65/65 tests verts. Monofichier 252 Ko (gzippé 80 Ko).
- **2026-06-20** : **module PI activé** (V1 réduit). TypeSelector carte PI désormais cliquable. ModeSelector restreint à Apprendre + S'entraîner pour PI (Défi et Puzzle viendront ensuite). Écran `PIPractice` avec sélecteur classe committed/stretch (radio), confiance avec range attendu adaptatif (80–100 % pour committed, 30–60 % pour stretch), valeur business 1–10. Écran `PILearn` avec 2 fiches pédagogiques (« Committed vs Stretch », « Valeur business — métadonnée ») et 5 exemples annotés (2 bons + 3 mauvais, validés DOMAINE §3.4). 65/65 tests verts. Monofichier 262 Ko (gzippé 83 Ko).
- **2026-06-20** : **Puzzle PI** ajouté. Corpus dédié dans `src/content/puzzles/pi.dev.fr.ts` : indicateurs business (SSO clients entreprise, taux de churn premium, NPS onboarding…), contextes train/PI (marketplace, segment premium, self-service…), repères PI (PI Review, fin de PI, PI Planning…). Catégories communes (verbes outcome, variations chiffrées, prépositions) reprennent les mêmes principes que Sprint. Écran `PIPuzzle` : duplication contrôlée de SprintPuzzle + bandeau « Métadonnées PI » (sélecteur committed/stretch + slider valeur business 1-10). Le draft assemblé est un `PiDraft` complet. 65/65 tests verts. Monofichier 275 Ko (gzippé 85 Ko). Note tech : extraction du `PuzzleBoard` en composant générique planifiée si un 3e usage apparaît (OKR).
- **2026-06-20** : retour Lætitia — challenge légitime sur la similarité des deux puzzles. Débat tenu sur 4 options architecturales avec arguments contre exposés (conflit D9, parasitage atelier, asymétrie modes, banalisation pédagogique). **Option B retenue en connaissance de cause** : Puzzle générique unique avec sélecteur de type interne. D16 consignée comme exception assumée à D9. Implémentation : `Puzzle.tsx` générique branché depuis Sprint et PI, sélecteur de type en tête (vide la zone à chaque bascule), métadonnées annexes conditionnelles (PI = committed/stretch + valeur business). Bascule de type synchronise le state app pour cohérence du fil d'Ariane. `SprintPuzzle.tsx` et `PIPuzzle.tsx` vidés (deprecated, à supprimer plus tard). 65/65 tests verts. Monofichier 269 Ko (gzippé 84 Ko).
- **2026-06-20** : retour Lætitia — outil ennuyeux, fiches pédagogiques trop chargées. Premier levier traité : **refonte des fiches en format infographique**. Nouveau champ `heroPhrase` (règle-clé en gros, fond teinté, bordure accent). Sections compactes (titre en uppercase muted, body court, bullets resserrées). Exemples mauvais/bon désormais en **cards visuelles côte à côte** (fond rouge pâle vs vert pâle, icônes). Contenu des 4 fiches existantes allégé : prose convertie en bullets courtes, sections fusionnées, redondances retirées. 65/65 tests verts. Monofichier 268 Ko (gzippé 84 Ko).
- **2026-06-21** : retour Lætitia — toujours « triste, gris ». 3 audits experts lancés en parallèle (game design adultes, direction visuelle, microcopy), rapports archivés dans `audits/2026-06-21-*`. Direction **« Editorial Tech : papier blanc, encres vives »** retenue (audit visuel). **Levier 1 livré** : nouvelle palette (off-white `#FAFAF7`, noir doux `#111`, focus indigo `#3730A3`, accent corail `#DC2626`, second souffle teal `#0F766E`, highlight jaune `#FEF3C7`), typo Georgia serif sur grands titres (h1/h2/sections), monospace sur scores, échelle 6 paliers 13/15/17/21/28/38, radius 6/10/14, shadows xs/sm/md, pictos passés à stroke 1.5, liseré accent au survol des cartes, motif bg-dots disponible. 65/65 tests verts. Monofichier 269 Ko (gzippé 84 Ko).
- **2026-06-21** : retour Lætitia — la palette ne suffit pas, la sensation vient du contenu et de la mécanique. **3 mockups inline présentés** (Défi scénarisé, Diff entre versions, Quiz trouve le piège), validés. **3 mécaniques livrées** : (B) **Diff entre versions** — fonction `diffEvaluations` pure (5 tests), affichage delta global + delta par critère dans `EvaluationPanel`, intégré dans Practice/Challenge/PIPractice avec mémoire `useRef` ; (A) **Cartes contexte scénarisées** — type `ScenarioCard`, corpus initial 6 cartes Sprint dev (Checkout SaaS B2B, Onboarding scale-up, Plateforme micro-services, Mobile backlog, Search start-up, Data ops), refonte complète de `SprintChallenge` qui pioche désormais dans le pool avec affichage carte contexte + bénéficiaire attendu ; (C) **Quiz « trouve le piège »** — composant `TrapHunt` tokenisant le texte cliquable, `AnnotatedExample.trapWords` ajouté aux 7 mauvais exemples Sprint, refonte de `SprintLearn` avec nouveau `BadExampleCard` qui présente le quiz par défaut. 70/70 tests verts. Monofichier 283 Ko (gzippé 87 Ko).
- **2026-06-21** : retour Lætitia — composition front « peu intelligente », demande d'expertise UI structurelle. Audit sous-agent dédié (rapport `audits/2026-06-21-audit-ui-structurelle.md`). Décision D17 : **système de gabarit `Screen` unifié** (header sticky / body 4 variantes / actions sticky bas à la Linear). Pilote livré sur `SprintPractice` : 5 composants de layout créés dans `src/ui/layout/`, écran refondu avec textarea principal hiérarchisé dans une `Zone primary` (paramètres en `<details>` repliable), `EvaluationPanel` en `Zone context` sticky, 3 actions d'export dans `ScreenActions` toujours visibles, statut centre affichant le score chip. 70/70 tests verts. Monofichier 290 Ko (gzippé 89 Ko). Si validé, propagation Challenge + Puzzle + Learn + Selectors + PIPractice en sessions suivantes.
- **2026-06-21** : retour Lætitia sur le pilote — fade, manque dynamisme. Mockups inline proposés (bleu pâle vs lavande). Choix validés : **fond lavande #F3F1FA**, **CTA primary corail #DC2626**, **score chip coloré selon statut** (vert/ambre/rouge), **consigne « à écrire ici ↓ » + textarea pointillé corail en état vide**. **Bug fix** : titre header était quasi invisible (couleur héritée) → ajout explicite `color: var(--color-text)` sur `.screen-header__title`. Score chip extrait en classe réutilisable `.score-chip` + variantes good/warn/bad. `--color-text-muted` ajusté pour rester AA sur lavande. 70/70 tests verts. Monofichier 291 Ko (gzippé 89 Ko).
- **2026-06-21** : **propagation du gabarit Screen** validée. 4 écrans supplémentaires migrés : SprintChallenge (header avec compteur + Tirer carte, body wide-rail avec ScenarioCard + textarea dans Zone primary + EvaluationPanel sticky, actions Quitter / score chip / Suivant primary), PIPractice (idem Sprint avec champs PI committed/stretch + business value), TypeSelector (single, eyebrow avec compte de types disponibles, microcopy retravaillé « Choisis ton terrain »), ModeSelector (single, microcopy « comment on démarre ? »). `App.tsx` étend la classe `.app__main--bare` via un helper `isScreenFrame()`. Reste à migrer : SprintLearn + PILearn (L, prochain incrément), Puzzle (M, prochain incrément). 70/70 tests verts. Monofichier 292 Ko (gzippé 89.5 Ko).
- **2026-06-21** : analyse multi-angles (pédagogue / graphiste / UX) des fiches pédagogiques. 5 améliorations livrées sur `SheetCard` : (1) **CTA d'application en bas** (« Va corriger un objectif mal posé › » primary corail) avec callback `onPractice` ; (2) **exemples pair-comparé interactifs** (bouton « Voir le score → » qui évalue via le moteur et affiche un score chip mini en place) ; (3) **pictos par section** (POURQUOI/SIGNAUX/COMMENT/etc. ont chacun leur icône corail, via nouveau champ `PedagogicalSection.icon`) ; (4) **encart Source différencié** (fond sable, italique, icône livre, via `kind: "source"`) ; (5) **marqueur position + bouton fiche suivante** (« Fiche 1 / 2 » dans le kicker + bouton « Fiche suivante › » qui scroll en douceur). Mise à jour des 4 fiches existantes avec icons + section source explicite. 70/70 tests verts. Monofichier 296 Ko (gzippé 90 Ko).
- **2026-06-21** : analyse honnête de la page d'accueil → 7 leviers appliqués. **A** cards compactes (`align-items: start`, padding réduit, suppression `min-height`) ; **B** CTA « Démarrer › » en corail uppercase sur chaque card active (avec micro-translation au survol) ; **C** pictos 32 px dans cercle accent 56 px (fond `color-mix` corail 10 %) ; **D** hero narratif (« Apprends à rédiger des objectifs solides en jouant. Choisis le cadre qui te concerne — on s'occupe du reste. ») ; **E** bannière `.explore-banner` sous les cards (« Pas encore prêt à choisir ? Découvre les fiches pédagogiques Sprint ») avec callback `onExplore` qui bascule directement sur Sprint → Apprendre ; **F** OKR retiré de la grille, banni en pied dans une `.coming-soon-banner` dashed plus discrète ; **G** padding vertical du shell header réduit (`--shell-header-h: 72px` au lieu de 88), eyebrow reformulé « Sprint et PI disponibles · OKR bientôt » (positif). 70/70 tests verts. Monofichier 298 Ko (gzippé 91 Ko).
- **2026-06-21** : analyse honnête du ModeSelector → 5 leviers appliqués. **A** propagation pictos cercle accent + CTA « Démarrer › » (régression corrigée — la classe `.card-button` partagée n'avait pas été propagée aux ModeCards) ; **B** eyebrow simplifié « Sprint · 4 modes disponibles » au lieu du redondant « Accueil · Sprint » ; **C** badge « Conseillé pour démarrer » en pill corail sur la card Apprendre ; **D** raccourci `explore-banner` « Déjà à l'aise ? Passe directement au Défi » (Défi pour Sprint, Puzzle pour PI/OKR) ; **E** regroupement « Pour aborder » (Apprendre + S'entraîner) / « Pour s'amuser » (Défi + Puzzle) avec sous-titres uppercase muted. 70/70 tests verts. Monofichier 300 Ko (gzippé 91 Ko).
- **2026-06-21** : trois corrections après retour utilisatrice. **(1)** Bug : badge « Conseillé pour démarrer » apparaissait en haut à droite de la page (position absolute mal contenue) → remplacé par placement dans le flux de la card avec `align-self: flex-start`. **(2)** Monochromie lavande lassante → introduction d'un **second accent teal** (`--color-accent-2: #0F766E`) sur le groupe « Pour s'amuser » via la classe `.card-button--play` (picto en cercle teal, CTA en teal, liseré accent teal). Shadows des cards passés de `xs` à `sm` pour mieux ressortir du fond lavande. **(3)** Tirets cadratins (—) retirés des titres et lédes visibles utilisateur : « Sprint, par où commencer ? », « Apprendre · Sprint », « PI, à venir », « Committed : engagement, 80 à 100 % de confiance », etc. Au passage, **bug CSS structurel découvert** : la refonte du `.card-button` (cercle picto, CTA, padding, shadow) n'avait jamais été persistée dans `components.css` au précédent commit — édit avait ciblé un bloc inexistant. Réparé en consolidant les classes `card-button`, `card-button__icon`, `card-button__cta`, `card-button--play` dans un seul Edit propre. 70/70 tests verts. Monofichier 302 Ko (gzippé 91 Ko).
- **2026-06-21** : **bug d'affichage critique fixé** — superposition du screen-header sur les cards lors du scroll (visible sur capture utilisatrice). Cause : `.app__header` avait `position: relative` dans `components.css` qui écrasait le `position: sticky` défini dans `layout.css` (les imports sont ordonnés tokens → layout → components). Conséquence : le shell header scrollait hors de vue, laissant le screen-header sticky à `top: 72px` flotter au-dessus du contenu sans masque. Fix : ajout de `position: sticky; top: 0; z-index: 30` directement dans la définition `components.css` du `.app__header`. Affecte **tous les écrans utilisant le gabarit Screen** (TypeSelector, ModeSelector, SprintPractice, SprintChallenge, PIPractice). 70/70 tests verts. Monofichier 302 Ko (gzippé 91 Ko).
- **2026-06-21** : nouveau jeu débutant **« Échauffement output/outcome »** (idée Lætitia, option C retenue). Domaine : type `WarmupCase` (level 1=verbe pur, 2=mini-objectif), port `getWarmupCases`. Contenu : 10 cas Sprint × dev validés (5 verbes purs « Livrer / Réduire / Refactorer / Permettre à / Déployer », 5 mini-objectifs avec contraste pédagogique sur « Améliorer » dans deux contextes opposés pour démontrer que le verbe seul ne tranche pas). Composant `Warmup.tsx` : machine à états intro → playing → level-transition → done, 2 gros choix « OUTPUT / OUTCOME » avec feedback immédiat coloré (vert/rouge) + explication courte, transition entre niveaux, récap final avec verdict contextuel selon score. Placement : en tête de `SprintLearn`, avant les fiches pédagogiques. 70/70 tests verts. Monofichier 312 Ko (gzippé 94 Ko).
- **2026-06-21** : **clarification chromatique** (retour Lætitia : couleurs en place portaient de la confusion). Diagnostic : `--color-accent` corail ≈ `--color-bad` rouge erreur ; `--color-accent-2` teal ≈ `--color-good` vert succès ; `--color-highlight` = `--color-warn-bg` (identiques). 5 rôles désormais 5 teintes distinctes : `accent #DC2626` corail (action) ≠ `bad #7F1D1D` rouge brique foncé (erreur) ; `accent-2 #A16207` ocre profond (action secondaire) ≠ `good #15803D` vert sapin (succès) ; `highlight #FFF8E7` ivoire ≠ `warn-bg #FDE6C5` ambre. Tous les rôles satisfont WCAG AA. **6 fixes UX Apprendre** : (A) `.trap-word` perd padding/margin — phrase reste fluide au lieu de bouts de mots détachés ; (B) hiérarchisation CTA via deux nouvelles classes `.btn--ghost` (bordure muted) et `.btn--link` (texte souligné) : « Voir pourquoi » devient ghost, « Voir le diagnostic moteur » devient link, « S'entraîner » descend en ghost ; (C) badges « Bon exemple » / « Trouve les pièges » retirés (redondants avec titres de colonne) ; (D) micro-indication pédagogique sous « Exemples annotés » (« Lis quelques bons exemples à gauche pour absorber le pattern, puis chasse les pièges à droite. ») ; (E) compteur sélection visible en live dans TrapHunt ; (F) section sticky reportée. 70/70 tests verts. Monofichier 313 Ko (gzippé 94 Ko).
- **2026-06-21** : **carrousel + extension PI complète**. Composant générique `CarouselColumn` (navigation un-à-un + récap final). Intégré dans **les deux colonnes** de SprintLearn et PILearn — fini la galerie qui scroll, place au parcours rythmé. **Extension PI au niveau Sprint** : (1) corpus Warmup PI (10 cas validés, dérivés du puzzle PI, verbes business « faire passer / atteindre / migrer ») ; (2) `trapWords` ajoutés sur 2 des 3 mauvais exemples PI (le 3e n'a pas de piège textuel — fallback texte simple + message explicatif) ; (3) corpus de 6 scénarios PI (trains SAFe, Business Owner, churn premium, marketplace pilot, plateforme oncall) ; (4) **`SprintChallenge.tsx` refondu en `Challenge.tsx` générique** paramétré par `type: ObjectiveType` (construit SprintDraft ou PiDraft avec valeurs par défaut raisonnables) ; (5) mode Défi **activé pour PI** dans ModeSelector ; (6) routage dans App.tsx. PI offre désormais les mêmes 4 modes que Sprint : Apprendre + S'entraîner + Défi + Puzzle. `SprintChallenge.tsx` deprecated. 70/70 tests verts. Monofichier 325 Ko (gzippé 97 Ko).
- **2026-06-21** : **cadrage module OKR**. Recherche doctrinale croisée (Morisseau via web, Lamorte et Niven via PDF dossier). Distinction faite avec ce que Lætitia rapprochait initialement de Sprint Goal puis de PI Objective : ce sont 3 étages distincts d'une **chaîne d'alignement vertical** (entreprise → équipe → PI → Sprint), chaque étage avec sa grammaire propre. **Doctrine refondue** dans DOMAINE.md §4 (validée Lætitia) : bi-niveau strict (entreprise annuel / équipe trimestriel), pas d'OKR individuel, distinction health metric ≠ KR (apport Lamorte), scoring d'atteinte hors périmètre V1. **Décisions architecturales** : D18 (deux types distincts dans la home : `okr-equipe`, `okr-entreprise`), D19 (page intermédiaire `LevelSelector` quand on clique OKR), D20 (V1 livre OKR équipe seulement, entreprise marqué « à venir » comme PI/OKR auparavant).
- **2026-06-21** : **Module OKR équipe livré (V1 : Apprendre + S'entraîner)**.
  **Domaine** : `ObjectiveType` scindé en 4 valeurs, `OkrDraft → OkrTeamDraft` (type `"okr-equipe"`), stub `OkrCompanyDraft` pour TypeScript. `CONFIDENCE_RANGES["okr-equipe"]` = 50-70 % (et stub entreprise idem). Trois pénalités OKR dans `criteria/okr.ts` : `objectiveQualitativePenalty` -20 si Objective porte un chiffre, `keyResultCountPenalty` -10 par KR manquant/en trop (plafond -20), `keyResultQualityPenalty` -25 par KR fautif (plafond -45, juge chiffré + pas verbe d'output). `evaluateCrediblyAmbitious` adapté avec `overrideConfidence` optionnel : pour OKR, le moteur passe la **moyenne des confiances KR** (l'Objective n'a pas de confiance propre). `evaluateFalsifiable` adapté : pour OKR, l'Objective est qualitatif par doctrine, le critère regarde si **au moins un KR** porte un chiffre. **Heuristique corrigée** (faux positif) : ajout des bigrammes idiomatiques outcome `faire passer / faire entrer / faire venir / faire adopter / faire de` dans `PHRASAL_OUTCOME_ALLOWANCES` — sans ça, « Faire passer le NPS de X à Y » et « Faire de [X] la [Y] » étaient pénalisés comme verbe d'output « faire ».
  **Contenu** : 2 fiches pédagogiques (« 1 Objective + 3-5 KR » avec le schéma de chaîne d'alignement vertical, et « Les 4 pièges du KR »), 7 exemples annotés (3 bons OKR complets + 4 mauvais ciblant chacun un piège). Le piège health-metric vit dans la fiche seulement (DOMAINE.md §4.4 : pas d'heuristique automatique en V1). 10 cas warmup output/outcome adaptés au niveau OKR.
  **UI** : 3 nouveaux écrans tous au gabarit Screen (D17) : `LevelSelector` (page intermédiaire OKR équipe / OKR entreprise « à venir »), `OkrTeamPractice` (formulaire Objective + N KR avec confidence par KR, ajout/retrait dynamique 1-5), `OkrTeamLearn` (warmup + fiches + carrousel bons/mauvais avec carte dédiée affichant O + KR + confidences). `TypeSelector` refondu : OKR redevient cliquable, bannière « Bientôt » retirée, eyebrow simplifié. `ModeSelector` étend `modesFor` aux deux niveaux OKR. `App.tsx` ajoute le state `okrFamilyPending` et le routing 3 niveaux Type → Niveau → Mode. Bredcrumb et `isScreenFrame()` couvrent le nouveau parcours.
  **Vérif** : 90/90 tests verts (12 nouveaux tests OKR + 8 fixtures OKR engine). TypeScript strict OK. Monofichier `coach-objectifs.html` 377 Ko (gzippé 106 Ko, +52 Ko / +20 Ko gzippés vs précédent).
  **Reste hors V1 OKR équipe** : Défi QCM OKR + Puzzle OKR (deuxième incrément). OKR entreprise complet (D20).
- **2026-06-22** : **dette gabarit Screen soldée**. Migration des 4 derniers écrans non migrés vers `<Screen>` (D17) : SprintLearn, PILearn, OkrTeamLearn et Puzzle. Pattern uniforme pour les Learn : header avec eyebrow + titre + lede + CTA secondaire « Passer à S'entraîner › » à droite ; body `variant: "single"` qui contient Warmup + fiches + carrousels. Pour Puzzle : `variant: "source-aside"` (1,6fr / 1fr-360min), le sélecteur de type d'objectif et le bouton « Vider » montent en `header.actions`, les colonnes de blocs vivent dans `primary`, la zone d'assemblage + phrase + EvaluationPanel dans `context` (sticky). Une barre `ScreenActions` apparaît en bas avec Quitter à gauche + score chip coloré au centre. `isScreenFrame()` simplifié dans App.tsx : Sprint/PI = tous modes, OKR équipe = practice + learn. 90/90 tests verts. Monofichier 380 Ko (gzip 107 Ko). Tous les écrans actifs sont désormais cohérents structurellement.
- **2026-06-22** : retour utilisatrice — bouton « Suivant » du Warmup trop discret après le fix précédent. Rééquilibré + 2 leviers de jouabilité ajoutés : (1) bouton avec fond teinté selon verdict (vert pâle si bonne réponse, brique pâle si mauvaise) + bordure assortie + micro-translation au survol — visible mais sobre, cohérent avec le code couleur sémantique ; (2) score en live dans le kicker (« Niveau 1 · Cas 2/5 · 3 ✓ sur 4 ») en typo mono ; (3) compteur de streak — à partir de 3 bonnes réponses d'affilée, affichage « Combo ×3 » en ocre dans le verdict. Reset à 0 dès une mauvaise réponse. Texte du dernier bouton plus narratif : « Passer au niveau 2 › ».
- **2026-06-22** : **Parité OKR équipe atteinte (modes Défi + Puzzle)**.
  **Défi QCM OKR équipe** : type `ChallengeQuizCase` étendu avec un champ optionnel `objectiveContext` qui, quand renseigné, fait afficher l'Objective trimestriel comme contexte avant le KR à reformuler. Pour Sprint/PI le champ reste absent, comportement inchangé. Corpus de 6 cas dans `content/challenge-quiz/okr-equipe.dev.fr.ts` — chacun cible un piège de KR distinct : KR-projet (verbe d'output), KR-flou (pas de métrique), health metric déguisée, sur-confiance (cible triviale = tâche), KR composite (« et »), KR sans avant/après (cible absolue). Composant `ChallengeQuiz` paramétré par `type: ObjectiveType` (au lieu de Sprint en dur). Routing dans App.tsx : `okr-equipe + challenge → ChallengeQuiz type=okr-equipe`. `ModeSelector` ouvre désormais les 4 modes pour `okr-equipe`.
  **Puzzle OKR équipe** : corpus dédié dans `content/puzzles/okr-equipe.dev.fr.ts` avec vocabulaire orienté outcome trimestriel (NPS, CSAT, adoption API, onboarding équipe, churn premium, etc.). Pédagogie : « Maintenir » est volontairement classé en **distracteur d'action** (piège health metric, cohérent avec la fiche pédagogique). Repères temporels = trimestre (fin du trimestre, fin de Q3, prochaine revue trimestrielle, 90 jours). Catégorie variation (tuiles chiffre) inchangée — la grammaire de variation est universelle. `AVAILABLE_TYPES` du Puzzle étend à `okr-equipe`. `buildDraft()` pour OKR équipe construit un vrai `OkrTeamDraft` avec 3 KR — le sentence assemblé en 1er, plus 2 KR repères pré-écrits pour respecter la contrainte 3-5 KR sans biaiser l'évaluation pédagogique du KR construit. Routing App.tsx étend l'accès au Puzzle pour OKR équipe.
  **Vérif** : 90/90 tests verts. Monofichier 395 Ko (gzip 110 Ko, +15 Ko / +3 Ko gzip vs précédent).
  **Reste hors V1** : Défi QCM PI (corpus à écrire). OKR entreprise complet (D20). **À étoffer** : nombre de fiches pédagogiques Sprint/PI/OKR équipe (mémoire utilisatrice).
- **2026-06-22** : **Parité Défi QCM atteinte sur les 3 modules**. Corpus PI Défi QCM créé (6 cas) dans `content/challenge-quiz/pi.dev.fr.ts` — chaque cas cible un piège distinct des PI Objectives : output technique (« Refactorer »), activité technique sans valeur (« Migrer vers… »), vœu pieux sans seuil sur un stretch, mot flou alors que la cible existe (committed), composite (« stabiliser et alléger »), variation floue (« de manière notable »). Contextes intégrant la dimension committed/stretch + rôle Business Owner + PI Review comme fenêtre de mesure + train d'équipes coordonnées. Routing App.tsx : PI Défi bascule de `Challenge` (rédaction libre) vers `ChallengeQuiz` — les 3 types Sprint/PI/OKR équipe utilisent désormais le même composant. `Challenge` (rédaction libre) reste exporté pour compat historique mais n'est plus atteignable depuis le routing. 90/90 tests verts. Monofichier 399 Ko (gzip 111 Ko, +4 Ko / +1 Ko gzip vs précédent).
  **Plan complété pour cette session** : (1) dette gabarit Screen soldée, (2) parité OKR équipe atteinte (4 modes), (3) parité PI atteinte (Défi QCM). Tous les modules ont les 4 modes en QCM.
  **Reste hors V1** : OKR entreprise complet (D20). **À étoffer** : nombre de fiches pédagogiques Sprint/PI/OKR équipe (mémoire utilisatrice).
- **2026-06-22** : **Warmup étoffé + ordre randomisé**. Corpus doublés sur les 3 modules : Sprint 10 → 20 cas (10 verbes purs + 10 mini-objectifs), PI 10 → 20 cas (verbes business, mini-objectifs PI Review), OKR équipe 10 → 21 cas (avec piège « Maintenir » sur health metric placé en verbe pur + 6 mini-KR couvrant rétention, refactor DB, intégration équipes, doc API). Composant `Warmup.tsx` : fonction `shuffle()` Fisher-Yates non destructive ; état `level1` / `level2` initialisé via `useState(() => shuffle(...))` (shuffle au premier mount) ; `reset()` re-shuffle à chaque rejouer. Shuffle **indépendant par niveau** pour préserver la progression pédagogique (verbes purs avant mini-objectifs). Textes intro et transition rendus paramétriques par `level1.length` / `level2.length`. Ajout d'une mention « L'ordre change à chaque lancement » dans l'intro pour signaler le comportement. 90/90 tests verts. Monofichier 407 Ko (gzip 112 Ko, +8 Ko / +2 Ko gzip vs précédent).
- **2026-06-22** : retour utilisatrice — « les fiches doivent être plus visibles, séparées des exercices ». **Refonte Apprendre en onglets Théorie / Pratique** (option A validée après mockup). Nouveau composant `LearnTabs` réutilisable (ARIA tablist/tab/tabpanel, sticky bar sous le screen-header, transition d'onglet sans remount, CTA « Passer à la pratique › » en bas du coin Théorie). 3 écrans Learn (SprintLearn, PILearn, OkrTeamLearn) refondus pour exposer `theory={…}` et `practice={…}` au composant. Théorie = fiches pédagogiques. Pratique = warmup + carrousels d'exemples annotés. Onglet « Théorie » actif par défaut (le nom Apprendre porte la promesse de lecture). 90/90 tests verts. Monofichier 446 Ko (gzip 122 Ko, +4 Ko / +1 Ko gzip vs précédent — coût négligeable).
- **2026-06-22** : retour utilisatrice — « la distinction Théorie/Pratique est un cran au-dessus, pas dans Apprendre ». **D21 actée** : refonte au niveau ModeSelector via onglets + palette différenciée + grille de thèmes + migration warmup/exemples vers Practice. **Domaine** : `LearningTheme`, champ `themeId` optionnel sur `PedagogicalSheet`, port `getThemes(type, audience)` qui regroupe les fiches et filtre les thèmes vides. **Contenu** : 2 thèmes par module (1 fiche chacun, à étoffer avec les 12 candidates issues de la recherche anti-patterns), fichier `content/themes.fr.ts` avec accroches courtes sans redondance avec le titre. **Tokens** : `--color-theory` (#1E40AF bleu marine), `--color-practice` (alias `--color-accent` corail), bandes de halo pâle pour les fonds (`--color-theory-bg`, `--color-practice-bg`). **UI** : composant `ThemeGrid` (cards thématiques cliquables avec picto, badge « Commence ici », accroche, liste des fiches, compteur, navigation retour) ; `ModeSelector` refondu en onglets Théorie/Pratique avec palette appliquée aux cards (`card-button--theory`, `card-button--practice`) ; 3 Learn (devenus écrans Théorie) ne contiennent plus que `ThemeGrid` ; 3 Practice deviennent un hub progressif en 3 étapes (warmup en accordéon ouvert, exemples annotés en accordéon fermé, formulaire toujours visible) — composants partagés `PracticeExamples` (Sprint/PI) et `OkrPracticeExamples` (variante OKR avec O + KR). Tirets cadratins éliminés des libellés visibles utilisateur (consigne Lætitia). 90/90 tests verts. Monofichier 452 Ko (gzip 123 Ko, +6 Ko / +1 Ko gzip vs précédent).
- **2026-06-22** : retour utilisatrice — « Défis QCM un peu trop faciles, je veux plus de cas avec plus de subtilités ». **Corpus doublés (6 → 12 cas par module)** avec nouveaux types de pièges plus fins :
  - **Sprint** (6 nouveaux) : métrique hors fenêtre temporelle (taux mensuel mesuré dans un sprint de 2 sem), vanity metric (téléchargements vs activation), cible irréaliste pour la fenêtre (diviser par 10 en 2 sem), indicateur lagging dépendant de tiers (conversion essai→payant) vs leading sous influence directe (clic « souscrire »), composite caché par « pour » causal (OAuth pour réduire l'abandon), métrique hors cadence (NPS trimestriel sur un sprint).
  - **PI** (6 nouveaux) : LTV (métrique 12 mois) inadapté à la fenêtre PI Review, outcome dépendant d'un autre train (décisions des équipes business), calibrage committed/stretch incohérent (100 % à 45 % de confiance classé committed), vanity metric (visiteurs) vs métrique de fin (GMV), effet de bord négatif sans guardrail (inscriptions sans qualité), KR de moyen (couverture de tests) vs KR de fin (MTTR).
  - **OKR équipe** (6 nouveaux) : KR sans baseline (NPS cible sans avant), confusion KR / Sprint Goal (mauvaise cadence), KR qui est en réalité un Objective déguisé, proxy vanity (clic en surface) vs métrique d'usage profond (3 sessions complétées), sur-confiance subtile (écart trivial à 100 % de confiance), composite caché (deux métriques dans le même KR).
  Chaque nouveau cas comporte une option **« partial »** qui ressemble à la bonne réponse mais avec un défaut subtil — l'utilisateur doit lire et arbitrer, pas reconnaître un patron évident.
  **Randomisation** : composant `ChallengeQuiz` étendu avec `shuffleCases()` Fisher-Yates — mélange l'ordre des **cas** ET l'ordre des **propositions** dans chaque cas (avec réaffectation des lettres A/B/C/D selon le nouvel ordre pour rester lisible). Shuffle au mount + re-shuffle au clic « Rejouer › ». L'utilisateur ne mémorise plus « la bonne est toujours la C ».
  90/90 tests verts. Monofichier 439 Ko (gzip 120 Ko, +32 Ko / +8 Ko gzip vs précédent).
